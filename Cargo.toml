[package]
name = "agi_sat"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.13.0", features = ["wgpu", "train", "autodiff"] }
burn-wgpu = "0.13.0"
rand = { version = "0.8.5", features = ["small_rng"] }
